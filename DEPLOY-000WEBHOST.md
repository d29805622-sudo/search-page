# 000webhost 部署详细指南

## 第一步：注册 000webhost 账号

1. 访问 https://www.000webhost.com/
2. 点击右上角 "Sign Up"（注册）
3. 使用 **邮箱** 注册账号（建议使用 Gmail 或 QQ 邮箱）
4. 验证邮箱后登录

---

## 第二步：创建新网站

1. 登录后，点击 **"Create Site"**（创建站点）
2. 输入网站名称，例如：`xiaomotou-forum`
3. 选择子域名，例如：`xiaomotou-forum.000webhost.com`
4. 点击 **"Add"**（添加）

---

## 第三步：进入文件管理器

1. 在网站管理面板，点击 **"Manage Website"**（管理网站）
2. 找到 **"Tools"**（工具）部分
3. 点击 **"File Manager"**（文件管理器）

---

## 第四步：上传网站文件

### 方法A：直接上传

1. 在文件管理器中，打开 `public_html` 文件夹
2. 点击左上角 **"Upload"**（上传）
3. 选择您下载的论坛代码文件夹
4. 上传所有文件

### 方法B：使用 ZIP 上传

1. 先将 `xiaomotou-forum` 文件夹压缩为 `xiaomotou-forum.zip`
2. 在文件管理器中上传 ZIP 文件
3. 右键点击 ZIP 文件，选择 **"Extract"**（解压）

---

## 第五步：创建数据库

1. 返回网站管理面板
2. 找到 **"Tools"** → **"Database"**（数据库）
3. 点击 **"Create Database"**（创建数据库）
4. 填写信息：
   - Database name: `xiaomotou_gal_forum`
   - Database user: `root`（系统会自动创建）
   - Database password: 系统会自动生成，请**复制保存**

⚠️ **重要**：记下以下信息，后面需要用到：
- 数据库名称
- 数据库用户名
- 数据库密码
- 数据库主机（通常是 `localhost`）

---

## 第六步：导入数据库表

1. 在 Database 页面，点击 **"phpMyAdmin"**
2. 点击左侧的数据库名称 `xiaomotou_gal_forum`
3. 点击顶部菜单 **"Import"**（导入）
4. 点击 **"Choose File"**（选择文件）
5. 选择项目中的 `database.sql` 文件
6. 点击页面底部的 **"Go"** 或 **"Import"**（导入）

✅ 导入成功后，应该看到 4 个表：
- category
- comment
- post
- user

---

## 第七步：修改配置文件

1. 返回文件管理器
2. 找到并打开 `config.php` 文件
3. 点击 **"Edit"**（编辑）
4. 修改数据库连接信息：

```php
$config = [
    'host' => 'localhost',
    'dbname' => 'xiaomotou_gal_forum',
    'username' => 'root',
    'password' => '这里填入您的数据库密码',
    'charset' => 'utf8mb4'
];
```

5. 点击 **"Save"**（保存）

---

## 第八步：测试网站

1. 在浏览器中访问您的网站，例如：
   ```
   https://xiaomotou-forum.000webhost.com/
   ```

2. 如果看到论坛首页，说明部署成功！

---

## 第九步：登录管理后台

1. 点击右上角 **"登录"**
2. 使用管理员账号：
   - 用户名：`admin`
   - 密码：`123456`
3. 登录后点击 **"管理后台"** 进入管理界面

---

## 常见问题

### Q1: 网站显示空白页
**解决方法**：
- 检查 `config.php` 数据库配置是否正确
- 确保数据库已正确导入

### Q2: 数据库连接失败
**解决方法**：
- 确认数据库名称、用户名、密码完全正确
- 确认主机是 `localhost`

### Q3: 文件上传失败
**解决方法**：
- 尝试使用 ZIP 压缩后上传
- 确保 `uploads` 文件夹有写入权限

### Q4: 找不到 phpMyAdmin
**解决方法**：
- 在网站管理面板仔细查找 "Database" 选项
- 可能需要在 "Settings" 中启用数据库

---

## 部署完成后的工作

1. ✅ **修改管理员密码**：登录后立即修改
2. ✅ **测试所有功能**：注册、发帖、评论、下载
3. ✅ **上传您的游戏包**：在对应板块发布带附件的帖子
4. ✅ **推广网站**：分享给您的朋友

---

## 技术支持

如果遇到问题，请检查：
- [ ] 数据库是否正确导入？
- [ ] config.php 配置是否正确？
- [ ] 所有文件是否上传到 `public_html`？
- [ ] 数据库用户名和密码是否匹配？

---

**恭喜！您的论坛已经成功部署！** 🎉

现在您可以开始使用了！